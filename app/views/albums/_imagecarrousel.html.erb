

<div class="container">
  <div class="gallery">
   <% @album.photos.each do |photo| %>
   <!---           <div class="item active"> -->
    <div class="gallery-img">
      <%= cl_image_tag photo.photo.path %>
    </div>
    <% end %>
  </div>
</div>
